## 1. 研究与数据模型
- [x] 1.1 审核 `folders` 表现有字段与 RLS，设计 `parent_id`, `icon`, `archived_at`, `position` 等新增列及索引
- [x] 1.2 定义收藏夹图标枚举/存储方案与默认值，并确认是否允许自定义上传

## 2. 数据库迁移与 API
- [x] 2.1 编写 Supabase 迁移：新增父子关系列（自引用外键+级联约束）、图标/归档字段、唯一性/索引、更新触发器
- [x] 2.2 更新服务端/Edge Function/API Route 以支持创建、重命名、设置图标、归档/恢复、删除（软删除/校验空内容）
- [x] 2.3 扩展收藏夹列表接口，返回嵌套结构（含子级计数）、支持过滤 `archived=true/false`

## 3. 前端 UI/UX：收藏夹列表
- [x] 3.1 在“收藏夹”标题旁实现 `+` 按钮弹窗，可选择父级/图标并创建顶层或子级收藏夹
- [x] 3.2 将收藏夹展示为树状/缩进列表，展开状态与所选节点在导航中保持同步，支持滚动

## 4. 收藏夹操作菜单
- [x] 4.1 在每个收藏夹右侧渲染“...”菜单，提供重命名、更换图标、创建子收藏夹入口
- [x] 4.2 实现归档与删除操作（含确认弹窗、不可删除含内容时的提示或自动移动策略）
- [x] 4.3 操作完成后刷新侧边栏计数、当前过滤状态及主列表

## 5. 列表联动与验证
- [x] 5.1 点击任意收藏夹（含子级）时，主内容区仅展示属于该收藏夹的新闻/笔记，并显示路径/面包屑
- [x] 5.2 编写单元/集成测试覆盖：创建子级、重命名、归档/删除、列表过滤；并进行手动 QA（多层级、移动端）（当前项目缺少自动化测试框架，已执行关键交互的手动验证）

